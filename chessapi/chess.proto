syntax = "proto3";

option go_package = "github.com/vivilCODE/chess/chessapi/protobuf";


message MakeMoveRequest {
    string body = 1;
}

message MakeMoveResponse {
    string body = 1;
}

service ChessApi {
    rpc MakeMove(MakeMoveRequest) returns (MakeMoveResponse) {};
}
